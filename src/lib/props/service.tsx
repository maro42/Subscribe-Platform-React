export type SearchByServiceName = {
    serviceName : string,
    pageNum : number,
    size : number
};

export type SearchByCategoryId = {
    categoryId : number,
    pageNum : number,
    size : number
}

export type BasicParam = {
    pageNum : number,
    size : number
}